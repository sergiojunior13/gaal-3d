// y = mx + b
export class LinearFunction {
    slope: number; // m
    y_intercept: number; // b

    constructor(slope: number, y_intercept: number) {
        this.slope = slope;
        this.y_intercept = y_intercept;
    }
}
